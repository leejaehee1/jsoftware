#spring boot 서버 주소와 포트 입력
server.address=localhost
server.port=8080

#콘솔에 SQL문을 출력해줄지 말지 결정
spring.jpa.show-sql=true

#DDL 데이터베이스 기능 사용
spring.jpa.generate-ddl=true

#Mysql
spring.jpa.database=mysql

# datasource
spring.datasource.hikari.jdbc-url=jdbc:mysql://localhost:3306/world?useSSL=false&characterEncoding=UTF-8&serverTimezone=UTC
spring.datasource.hikari.username=root
spring.datasource.hikari.password=rkd!ckd0316
spring.datasource.hikari.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.hikari.maximum-pool-size=4

spring.jpa.database-platform=org.hibernate.dialect.MySQL5InnoDBDialect

# session redis
spring.session.store-type=redis
spring.redis.host=localhost
spring.redis.password=password
spring.redis.port=6379

# security
spring.security.user.name=sergey
spring.security.user.password=sergey
spring.security.user.roles=manager

# MyBatis
# mapper.xml 위치 지정
#mybatis.mapper-locations=classpath:/sqlmap/*.xml

# model 프로퍼티 camel case 설정
mybatis.configuration.map-underscore-to-camel-case=true

# 패키지 명을 생략할 수 있도록 alias 설정
mybatis.type-aliases-package=product

# mapper 로그레벨 설정
logging.level.com.jsoftware.model.repository=TRACE